import { Cards, Card, Callout } from "nextra/components";
import Image from "next/image";
import ImageView from "../../components/imageView";

## Writing data to NFC labels

To write data to NFC tag, select an item from a survey, press 'Create label' and then select 'Write to NFC tag'

<ImageView>
  <Image
    src="/images/nfc/10.2.png"
    alt="NFC tag view"
    width={300}
    height={300} />
  <Image
    src="/images/nfc/10.3.png"
    alt="NFC tag view"
    width={300}
    height={300} />
</ImageView>

Bring your phone close to the NFC label and wait until the 'Completed' message appears. If errors are encountered during writing, press 'Retry' to start over.
